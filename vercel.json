{
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "installCommand": "unzip -o src.zip && unzip -o public.zip && npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
